<!DOCTYPE html>
<html>
<head>
  <title>Arduino Jossean</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: Arial;
      text-align: center;
      margin-top: 60px;
    }
    h1 {
      color: #ff8800;
      font-size: 32px;
    }
    h2 {
      font-size: 50px;
      margin-top: 20px;
    }
  </style>
</head>
<body>

  <h1>Arduino Jossean</h1>
  <h2 id="temp">Cargando...</h2>

<script type="module">
import { initializeApp } from "https://www.gstatic.com/firebasejs/10.0.0/firebase-app.js";
import { getDatabase, ref, onValue } from "https://www.gstatic.com/firebasejs/10.0.0/firebase-database.js";

const firebaseConfig = {
  databaseURL: "https://arduinojossean-c0c82-default-rtdb.firebaseio.com/"
};

const app = initializeApp(firebaseConfig);
const db = getDatabase(app);

onValue(ref(db, "arduinojossean/temperatura"), (snapshot) => {
  document.getElementById("temp").innerText = snapshot.val() + " °C";
});
</script>

</body>
</html>