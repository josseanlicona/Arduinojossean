<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Arduino Jossean</title>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
        import { getDatabase, ref, onValue } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-database.js";

        const firebaseConfig = {
            apiKey: "AIzaSyDnSVoZ_1HEQzU-0eqgk7LI-AsdTfaxW6U",
            authDomain: "arduinojossean-c0c82.firebaseapp.com",
            databaseURL: "https://arduinojossean-c0c82-default-rtdb.firebaseio.com",
            projectId: "arduinojossean-c0c82",
            storageBucket: "arduinojossean-c0c82.appspot.com",
            messagingSenderId: "968610073834",
            appId: "1:968610073834:web:7726de2ed593e383d9e32c"
        };

        const app = initializeApp(firebaseConfig);
        const db = getDatabase(app);

        const tempRef = ref(db, "Temperatura");

        onValue(tempRef, (snapshot) => {
            const valor = snapshot.val();
            document.getElementById("temp").innerHTML =
                valor !== null ? valor + " °C" : "Sin datos";
        });
    </script>

    <style>
        body { font-family: Arial; text-align: center; margin-top: 80px; }
        h1 { color: orange; }
        #temp { font-size: 60px; font-weight: bold; }
    </style>
</head>
<body>
    <h1>Arduino Jossean</h1>
    <div id="temp">Cargando...</div>
</body>
</html>
